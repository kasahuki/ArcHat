# .env.production  
# 生产环境可以不设置这个变量，代码会自动构建正确的 URL
# 或者如果你有特定的生产环境 WebSocket 服务器，可以这样设置：
# VITE_WEBSOCKET_URL=wss://your-production-domain.com/ws
#    url: `${location.protocol === 'https:' ? 'wss' : 'ws'}://${location.host}/ws/`, 
#    url: `ws://localhost:8090/`,
 